/**
 * Public marketing feature flags.
 * Flip these on when ready to announce / re-surface products.
 */
export const features = {
  /** Utility Pilot — hidden until the product launch announcement. */
  utilityPilot: false,
} as const
